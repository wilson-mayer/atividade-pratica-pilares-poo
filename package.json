{
  "name": "atividade-pratica-pilares-poo",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc && node ./dist"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
